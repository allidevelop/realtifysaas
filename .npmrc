# ESLint (legacy-резолюция плагинов через FlatCompat) не находит вложенные
# плагины eslint-config-next под строгим node_modules pnpm — хойстим их.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=@next/eslint-plugin-next
