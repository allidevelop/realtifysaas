# DECISIONS.md

Журнал технических решений. Формат: **дата · решение · альтернативы · причина**. Новые записи — сверху.

---

### 2026-06-15 · Этап 1 · UI-стек: Tailwind v4 + typography
- **Решение:** `tailwindcss@4.3.1` + `@tailwindcss/postcss@4.3.1` (конфиг в CSS, `@theme`-токены), `@tailwindcss/typography@0.5.20` для richtext (`prose`), `clsx@2.1.1`. Tailwind применяется только в группе `(frontend)`; админка Payload — свои стили.
- **Альтернативы:** Tailwind v3 + JS-конфиг; shadcn/ui сразу.
- **Причина:** v4 актуальна и проще (CSS-first). shadcn/ui наложим позже при усложнении UI (ТЗ §5).

### 2026-06-15 · Этап 1 · Локализация Payload uk/en включена на старте
- **Решение:** `localization: { locales: [uk, en], defaultLocale: 'uk', fallback: true }`; ключевые текстовые поля помечены `localized: true`. Интерфейс фронта пока uk; `next-intl` для UI — позже.
- **Альтернативы:** добавить локализацию позже.
- **Причина:** ретрофит локализации в Payload требует миграции данных — дешевле заложить сразу (ТЗ §13). UI-i18n не входит в DoD этапа 1, делаем отдельным проходом.

### 2026-06-15 · Этап 1 · SEO через @payloadcms/plugin-seo
- **Решение:** `seoPlugin@3.85.1` добавляет группу `meta` (title/description/image) в pages/news/tools; на фронте — `generateMetadata` (canonical/OG/Twitter), `sitemap.ts`, `robots.ts` (в корне `src/app/`), JSON-LD (Article/Product+Offer).
- **Причина:** канонический путь Payload; покрывает ТЗ §13. **Важно:** `robots.ts`/`sitemap.ts` должны лежать в корне `app/`, а не в route-группе `(frontend)` — иначе `/robots.txt` отдаёт 404.

### 2026-06-15 · Этап 1 · Pages.section разделяет /slug и /legal/slug
- **Решение:** в Pages поле `section` (general|legal). `/[slug]` рендерит general, `/legal/[slug]` — legal; перекрёстный доступ закрыт `notFound()`.
- **Причина:** одна коллекция Pages для всех статичных страниц, но чистые URL для юр-документов (ТЗ §8.1).

### 2026-06-15 · Этап 1 · Seed выполняется через route /seed, не `payload run`
- **Решение:** логика наполнения — в `src/seed/seed.ts` (`runSeed(payload)`); вызывается из (а) `payload run src/seed/index.ts` и (б) `GET /seed?secret=$PAYLOAD_SECRET` (рантайм Next).
- **Причина:** `payload run` использует `tsx@4.22.4`, который на **Node 26 (Windows)** падает с `ENOENT node:crypto?tsx-namespace` (баг резолва node:-билтинов; это последняя версия tsx, фикса нет). Route работает в рантайме Next без tsx. Гард по секрету; идемпотентно. При совместимой версии Node остаётся доступен и `pnpm --filter web seed`.

### 2026-06-15 · Этап 1 · PostGIS spatial_ref_sys в схеме gis — критично для Payload push
- **Решение (подтверждено на практике):** держать PostGIS в схеме `gis` (см. запись этапа 0). При сбросе/изменении схемы Payload интерактивный drizzle-push не зависает, т.к. `public` чистая.
- **Причина:** при значимых изменениях схемы public сбрасывали через `DROP SCHEMA public CASCADE; CREATE SCHEMA public;` (gis не трогаем) — Payload пересоздаёт таблицы без вопросов.

### 2026-06-14 · Этап 0 · Зафиксированные версии стека
- **Решение:** установлены и закреплены версии:
  - web: `next@15.4.11`, `react@19.2.7`, `react-dom@19.2.7`, `payload@3.85.1` и `@payloadcms/{next,db-postgres,richtext-lexical}@3.85.1`, `graphql@16.14.2`, `sharp@0.35.1`; tooling `typescript@5.7.3`, `eslint@9.18.0`, `eslint-config-next@15.4.11`.
  - engine: `python>=3.12` (локально 3.13.5), `fastapi`, `uvicorn[standard]`, `pydantic@2`, `pydantic-settings`; dev: `ruff`, `mypy`, `pytest`, `httpx`. Менеджер — `uv`.
  - инфра: `postgis/postgis:16-3.4`, `redis:7-alpine`.
- **Причина:** воспроизводимость; единая версия всех `@payloadcms/*` обязательна.

### 2026-06-14 · Этап 0 · Next.js 15, не 16 (несмотря на доступность)
- **Решение:** берём `next@15.4.11` (последняя 15.x в полосе, разрешённой peer-范围 Payload 3.85.1). На момент работы вышел `next@16.2.9`, и Payload его поддерживает.
- **Альтернативы:** сразу перейти на Next 16.
- **Причина:** ТЗ §5 фиксирует Next.js 15; мажор-переход — отдельная оценка (как и предписывает запись про мажоры ниже). Кандидат на апгрейд в будущем.

### 2026-06-14 · Этап 0 · PostGIS ставится в схему `gis`, а не `public`
- **Решение:** `CREATE EXTENSION postgis SCHEMA gis` (а не в `public`); `search_path = public, gis`.
- **Альтернативы:** PostGIS в `public` (дефолт `CREATE EXTENSION`).
- **Причина:** Payload (drizzle push) интроспектирует `public` и, увидев чужую таблицу `spatial_ref_sys`, зависает на интерактивном вопросе о переименовании. Размещение PostGIS в `gis` оставляет `public` чистой под Payload — и это ровно разделение схем из ТЗ §6 (public=Payload, gis=PostGIS).

### 2026-06-14 · Этап 0 · Хост-порт Postgres — 5433
- **Решение:** контейнер маппится `5433:5432`; `DATABASE_URL` использует `localhost:5433`.
- **Альтернативы:** стандартный 5432.
- **Причина:** на машине разработки запущена служба `postgresql-x64-17`, занимающая 5432 и перехватывающая TCP-подключения. 5433 убирает конфликт, не трогая существующую установку пользователя.

### 2026-06-14 · Этап 0 · Корневой `.env` загружается в web через `next.config.mjs`
- **Решение:** Next по умолчанию читает `.env` из каталога приложения; секреты лежат в корневом `.env` монорепо (ТЗ §17). В `apps/web/next.config.mjs` подгружаем корневой `.env` через `process.loadEnvFile('../../.env')` (Node ≥20.12).
- **Альтернативы:** дублировать `.env` в каждом приложении.
- **Причина:** единый источник секретов на монорепо; без этого Payload падал с «missing secret key».

### 2026-06-14 · Этап 0 · ESLint-плагины хойстятся через `.npmrc`
- **Решение:** `public-hoist-pattern[]=*eslint*` в корневом `.npmrc`.
- **Причина:** `next lint` через FlatCompat использует legacy-резолюцию плагинов, которая не находит вложенные зависимости `eslint-config-next` под строгим `node_modules` pnpm.

### 2026-06-14 · Коммерческая версия (заменяет трактовку «личное использование»)
- **Решение:** проект — коммерческий продукт (конкурент gisuvecon.com) с монетизацией через подписки и продажу аналитики. «Личное использование» ранее означало лишь «не заказ клиента», а не «некоммерческое». Источник истины — `TZ-geoanalytics-uvecon-v2-commercial.md`.
- **Следствие:** монетизация, биллинг, юр-часть и стратегия данных — обязательные требования, не флаги.
- **Отменяет:** записи ниже про «эквайринг локально не нужен» и «скрейпинг не реализуем» в части их обоснования «личным» проектом (см. уточнения там же).

### 2026-06-14 · Платежи и биллинг — первоклассное требование
- **Решение:** строим рекуррентные подписки (LiqPay/WayForPay/Fondy/Monobank) + безнал по счёту для юрлиц/банков (рахунок-фактура + акт). Доступ к инструментам гейтится по тарифу. Локально пока не настраиваем (этап 5), но это в scope.
- **Альтернативы:** ручная активация без эквайринга.
- **Причина:** доход = подписки и продажи; банки платят только по безналу.
- **Заменяет** прежнее «платежи локально не нужны»: причина была неверной (трактовка проекта как некоммерческого).

### 2026-06-14 · Стратегия данных — на легитимных источниках; накопление своего ряда
- **Решение:** данные объявлений берём из легитимных источников Уровня 1 (официальные API/партнёрства площадок — проверить условия; открытые госреестры; покупка данных) и **накапливаем собственный временной ряд с момента запуска**. Историческую глубину конкурента задним числом не воспроизводим.
- **Скрейпинг чужих сайтов** — НЕ дефолт: юридический риск (ToS, перс. данные, авторские права), решение принимать только с юр-консультацией и фиксировать здесь с оценкой рисков.
- **Альтернативы:** массовый скрейпинг 130+ сайтов «как у конкурента».
- **Причина:** данные — ядро бизнеса и главный юр-риск; легитимный путь устойчивее. Это уточняет (не отменяет цель) прежнюю запись про открытые данные: для коммерции открытых границ мало, нужен реальный поток объявлений.

### 2026-06-14 · Нужен угол дифференциации (поздний игрок)
- **Решение:** до разработки ядра зафиксировать нишу/позиционирование (узкий сегмент / регион / API-first / UX / свежесть данных). Лобовая копия не делается.
- **Причина:** против укоренившегося лидера «то же самое» необходимо, но недостаточно.
- **TODO:** записать выбранный угол отдельной записью, когда определишься.

### 2026-06-14 · Разработка сейчас локально; прод — отдельная веха (НЕ отменено)
- **Решение:** ранние этапы — локально (docker compose для БД/Redis, приложения на хосте). Прод-инфра (Nginx/PM2/SSL/бэкапы/мониторинг) — этап 7 roadmap, обязательный.
- **Причина:** быстрее итерации; но в отличие от прежней формулировки прод теперь — реальная цель, а не «отложено навсегда».

### 2026-06-14 · Монорепо из двух приложений + общая БД с двумя схемами
- **Решение:** `apps/web` (Next.js+Payload) и `apps/engine` (Python) в одном репозитории (pnpm workspaces + uv); одна PostgreSQL, схемы `public` (Payload) и `gis` (PostGIS).
- **Причина:** синхрон контрактов и общий бэкап; изоляция гео-данных схемой без второй БД.

### 2026-06-14 · Стек: Next.js 15 + Payload 3 + PostGIS + FastAPI
- **Решение:** фронт/CMS — Next.js 15 (App Router, React 19, TS) + Payload CMS 3 (Postgres/Drizzle); гео — PostgreSQL 16 + PostGIS 3.4; расчёты/ETL/бот/публичный API — Python 3.12 (FastAPI, aiogram 3).
- **Причина:** предпочтения владельца, headless-архитектура, удобство ведения через Claude Code.
- **TODO при установке:** зафиксировать точные версии; при новых мажорах (Next 16 / Payload 4) — оценить и записать.

### 2026-06-14 · Карта на Leaflet, MapLibre — запасной путь
- **Решение:** choropleth на Leaflet через `next/dynamic({ssr:false})`. Порог перехода на MapLibre GL — >~5–7 тыс. полигонов на экране с тормозами.
- **Причина:** проще для старта, достаточно для область/район.
